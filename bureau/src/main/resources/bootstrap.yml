# service name
spring:
  application:
    name: bureau
# config server location
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: ${activatedProperties}
#      username: admin
#      password: admin1234
#      failFast: true
